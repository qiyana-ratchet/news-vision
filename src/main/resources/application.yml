spring:
    h2:
        console:
            enabled: true
            path: /h2-console
            settings:
                web-allow-others: true
    datasource:
        driver-class-name: org.h2.Driver
        url: jdbc:h2:~/news-vision-db
        username: news
        password:
    sql:
        init:
            mode: always
            schema-locations: classpath:schema.sql
            data-locations: